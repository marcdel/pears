{
  "name": "pears-e2e",
  "private": true,
  "scripts": {
    "test": "npx playwright test",
    "test:external": "npx playwright test --config=playwright.external.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
